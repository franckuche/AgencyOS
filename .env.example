# Claude CLI — chemin vers le binaire (optionnel si claude est dans le PATH)
# CLAUDE_BIN=/usr/local/bin/claude

# Agents — dossier contenant les agents (default: ./agents)
# AGENTS_DIR=./agents

# Clients — dossier contenant les dossiers clients (default: ./clients)
# CLIENTS_DIR=./clients
